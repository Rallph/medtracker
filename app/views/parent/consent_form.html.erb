
<h2 style="margin: auto;width: 50%;text-align: center;border: 3px black;padding: 80px;font-weight: bold;"> Approved Medications for: John Doe </h2>


<table id="poll_table" style="width:400px;margin:0 auto;font-size:20px;position:relative;top:-40px;table-layout: fixed" class="table table-striped table-sm">
  <thead>
  <tr>
    <th scope="col" style="width:200px">Medication Name</th>
    <th scope="col" style="width:400px">Approval Notes</th>
    <th scope="col" style="width:200px"></th>

    <!--    <th scope="col" >Phone Number</th>-->
  </tr>
  </thead>

  <tbody>

  <% @approved_student_meds.each do |asm| %>
<!--    <p> <%#= asm %></p>-->
    <tr class="active">
      <td><%= asm.medication_name %></td>
      <td></td>
      <td align="right"><%= link_to "<span class='glyphicon glyphicon-remove'></span>".html_safe, "delete_invitee?delete_invitee=", {class: 'btn btn-danger', type: 'button'} %></td>
    </tr>
  <% end %>

    <tr class="active">
      <td>
      <div class="input-group">
        <select class="custom-select" style="width:200px" id="inputGroupSelect04">
          <option selected></option>
          <% @non_approved_student_meds.each do |nasm| %>
            <option value="1"><%= nasm.medication_name %></option>
        <% end %>
        </select>
      </div>
      </td>
      <td></td>
      <td align="right"><%= link_to "Approve Medication".html_safe, "delete_invitee?delete_invitee=", {class: 'btn btn-primary', type: 'button'} %></td>
    </tr>
</table>
